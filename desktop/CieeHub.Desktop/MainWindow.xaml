<Window x:Class="CieeHub.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="CIEE Hub Desktop 0.2" Height="900" Width="1600" MinHeight="720" MinWidth="1180"
        Background="#0F172A" Foreground="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="520"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#111827" Padding="12,8">
                <DockPanel>
                    <TextBlock Text="CIEE HUB 4.1.4 • DESKTOP" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="HubStatus" DockPanel.Dock="Right" Text="Inicializando..." Foreground="#93C5FD" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>
            <wv2:WebView2 x:Name="HubView" Grid.Row="1"/>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="#334155"/>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="150"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#111827" Padding="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Kairós" Width="76" Margin="0,0,6,0" Click="Kairos_Click"/>
                    <Button Grid.Column="1" Content="Zendesk" Width="82" Margin="0,0,6,0" Click="Zendesk_Click"/>
                    <Button Grid.Column="2" Content="Talentos" Width="82" Margin="0,0,8,0" Click="Talentos_Click"/>
                    <TextBox x:Name="AddressBox" Grid.Column="3" VerticalContentAlignment="Center" Margin="0,0,6,0" KeyDown="AddressBox_KeyDown"/>
                    <Button Grid.Column="4" Content="Ir" Width="56" Click="Go_Click"/>
                </Grid>
            </Border>
            <TabControl x:Name="BrowserTabs" Grid.Row="1" SelectionChanged="BrowserTabs_SelectionChanged" Background="#0B1220" BorderThickness="0"/>
            <Border Grid.Row="2" Background="#020617" BorderBrush="#334155" BorderThickness="0,1,0,0" Padding="8">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="LOG DO DESKTOP" FontWeight="Bold" Foreground="#93C5FD"/>
                        <Button Content="Limpar" Margin="12,0,0,0" Padding="8,1" Click="ClearLog_Click"/>
                    </StackPanel>
                    <TextBox x:Name="LogBox" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto" Background="#020617" Foreground="#CBD5E1" BorderThickness="0"
                             FontFamily="Consolas" FontSize="11" TextWrapping="NoWrap"/>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
